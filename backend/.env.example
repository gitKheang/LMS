# MongoDB Connection
# For local development with Docker: mongodb://localhost:27017
# For MongoDB Atlas: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/
MONGODB_URI=mongodb://localhost:27017
DATABASE_NAME=library

# JWT Secret (REQUIRED - generate a strong random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS (comma-separated for multiple origins)
# For production, add your Vercel frontend URL
# Example: https://library-frontend.vercel.app,http://localhost:4200
ALLOWED_ORIGINS=http://localhost:4200

# Admin credentials for seeding (optional)
ADMIN_EMAIL=admin@library.edu
ADMIN_PASSWORD=admin123
ADMIN_NAME=Library Admin
